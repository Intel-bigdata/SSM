# These are test cases which specify file size and num. The base sync dir name is "size_num".
# The files for these case should be created under them in advance.

# Test cases, e.g., declare -A CASES=(["10MB"]="10" ["100MB"]="10")
declare -A CASES=([10MB]="10")

# SSM home, e.g., SMART_HOME=~/smart-data-1.4.0
SMART_HOME=/home/hdfs/ssm/smart-data-1.6.0-SNAPSHOT

# PAT home, e.g., PAT_HOME=~/PAT
PAT_HOME=/home/hdfs/PAT-master

# Hadoop home, e.g., HADOOP_HOME=~/hadoop
HADOOP_HOME=/usr/hdp/3.0.1.0-187

# HiBench home, e.g., HiBench_HOME=~/HiBench
HiBench_HOME=/home/hdfs/HiBench-master

# The cluster hdfs url for ec performance test, e.g., SRC_CLUSTER=hdfs://sr613:9000
SRC_CLUSTER=hdfs://ssm1:8020

# The dest dir to store the data converted to ec by distcp, e.g., DEST_DIR_EC=/dest/ec
DEST_DIR_EC=/dest/ec

# The dest dir to store the data converted to replica by distcp, e.g., DEST_DIR_REPLICA=/dest/replica
DEST_DIR_REPLICA=/dest/replica

# The namenode's hostname of src cluster
SRC_NODE=ssm1

# The hosts require dropping cache, e.g., HOSTS="host1 host2"
HOSTS="ssm1 ssm2 ssm3"

# The number of mapper for distcp, e.g., MAPPER_NUM="30 60 90".
# For the sake of fair comparison, please make sure this value is consistent with the overall cmdlet executors of SSM.
MAPPER_NUM=30


